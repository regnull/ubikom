package imap

import (
	"testing"
)

func Test_NewMailbox(t *testing.T) {
	// assert := assert.New(t)

	// mb, err := NewMailbox("foo", "bar", nil)
	// assert.NoError(err)
	// assert.EqualValues("foo", mb.User())
	// assert.EqualValues("bar", mb.Name())
	// status, err := mb.Status(nil)
	// assert.NoError(err)
	// assert.True(status.UidValidity >= 1000)
}
